# Kafka observation — 2026-08-27 — HRP-29

**Observed by:** Miguel
**Reviewer:** Pending (Gaby)
**Authorised environment:** Local educational Kafka runtime
**Method:** Bounded, in-memory metadata and structural observation
**Restriction confirmed:** The educational data generator was not read, inspected or analysed.

## Session summary

| Item | Observation |
|---|---|
| Date | 2026-08-27 |
| Topic observed | `probando` |
| Messages observed | 20 |
| Partitions observed | 1 (partition `0`) |
| Valid JSON objects | 20 |
| Invalid or unusable messages | 0 |
| Technical errors | 0 |
| Offset sequence | Strictly increasing in the observed sequence |

The observer did not write offsets, commit consumer state, persist payloads or print
message values.

## Structural variants observed

Five distinct top-level field sets were observed. The labels below are technical
variants, not business classifications.

| Variant | Count in sample | Fields observed | Notes |
|---|---:|---|---|
| A | 7 | `IPv4`, `address` | All observed values had apparent type `string`. |
| B | 4 | `company`, `company address`, `company_email`, `company_telfnumber`, `fullname`, `job` | All observed values had apparent type `string`. |
| C | 4 | `IBAN`, `passport`, `salary` | All observed values had apparent type `string`. |
| D | 3 | `address`, `city`, `fullname` | All observed values had apparent type `string`. |
| E | 2 | `email`, `last_name`, `name`, `passport`, `sex`, `telfnumber` | `sex` had apparent type `array`; the other observed fields had apparent type `string`. |

No values, keys, identifiers or payload excerpts are recorded in this document.

## Field structure

Every listed field was absent from at least one other structural variant. No JSON
`null` value was observed for a field while it was present in this bounded sample.

| Field | Apparent type | Present in sample | Variants |
|---|---|---:|---|
| `IBAN` | string | 4/20 | C |
| `IPv4` | string | 7/20 | A |
| `address` | string | 10/20 | A, D |
| `city` | string | 3/20 | D |
| `company` | string | 4/20 | B |
| `company address` | string | 4/20 | B |
| `company_email` | string | 4/20 | B |
| `company_telfnumber` | string | 4/20 | B |
| `email` | string | 2/20 | E |
| `fullname` | string | 7/20 | B, D |
| `job` | string | 4/20 | B |
| `last_name` | string | 2/20 | E |
| `name` | string | 2/20 | E |
| `passport` | string | 6/20 | C, E |
| `salary` | string | 4/20 | C |
| `sex` | array | 2/20 | E |
| `telfnumber` | string | 2/20 | E |

## Correlation, ordering and idempotency

| Aspect | Observation | Certainty |
|---|---|---|
| Candidate fields for correlation | `passport`, `fullname` and `address` occur in more than one variant. Equality of their values and their business meaning were not assessed. | Observed names; correlation rule pending |
| Technical order | Kafka offsets were strictly increasing for the bounded single-partition sample. | Observed |
| Business order across variants | The sample does not prove a business ordering or a complete-person sequence. | Pending |
| Duplicate Kafka records | No repeated topic/partition/offset appeared in the sample. Business-level duplicates were not assessed. | Technical observation only |
| Incomplete person groups | Not assessed: values were deliberately not retained or compared. | Pending |

## Differences from provisional published documentation

The observed structural names include `last_name`, `company address` and
`company_email`. These must be treated as observed facts for HRP-24. They are not
automatically equivalent to similarly named fields in the public project README.

`sex` was observed as an `array`, and `salary` was observed as a `string` in this
sample. Their semantics and validation rules remain unknown.

## Handoff to HRP-24

Facts available to Gaby:

- One observed topic, one partition and five structural variants.
- The safe field inventory and apparent types above.
- Candidate correlation field names, with no approved joining rule.
- No invalid JSON or technical consumer error in this bounded session.

Still unknown:

- Field meanings, canonical naming and required/optional business rules.
- The final correlation key and conflict-resolution rules.
- Ordering guarantees between business variants.
- Business-level duplicates and completeness of records for a person.

HRP-24 must record these items as `unknown` or pending rather than infer them.

## Execution evidence

- Tooling: authorised Kafka metadata query plus a bounded in-memory observer.
- Persistence: none; no payload, key, secret or `.env` content was written.
- Related task: HRP-29.
- Related pull request: #2.
- Required human review: Gaby.
