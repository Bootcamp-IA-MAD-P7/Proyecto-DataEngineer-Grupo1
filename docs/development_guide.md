# Development guide

The team onboarding and daily commands live in
[`onboarding/ai-assisted-workflow.md`](onboarding/ai-assisted-workflow.md). This
document is the stable entry point expected by the Specboot structure.

Use `develop` as the integration branch. Create a task branch, follow the SDD flow,
run the quality harness and use a reviewed PR for every change.
