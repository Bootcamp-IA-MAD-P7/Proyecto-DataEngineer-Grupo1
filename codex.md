# Codex project context

Read and follow [`AGENTS.md`](AGENTS.md), then use the task-specific material in `docs/ai/`, `docs/specs/` and `docs/adr/`. The project workflow is SDD-first and requires a human reviewer for every merge.
